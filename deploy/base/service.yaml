apiVersion: v1
kind: Service
metadata:
  name: portfolio-backend
spec:
  # Default to ClusterIP so the service is only reachable inside the cluster.
  type: ClusterIP
  selector:
    app: portfolio-backend
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
